import 'package:dewsclim/lib.dart';

@RoutePage(name: 'genderReg')
class GenderRegScreen extends StatelessWidget {
  const GenderRegScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
